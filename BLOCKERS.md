# BLOCKERS

- No active blockers.

## Resolved

- DMG packaging initially failed inside the sandbox at `bundle_dmg.sh`, but passed when rerun with unsandboxed approval because macOS disk-image tooling needs broader system access.
